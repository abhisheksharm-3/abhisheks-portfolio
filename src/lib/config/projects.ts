/**
 * Defines the delay timings for staggered animations within the Projects section.
 */
export const PROJECT_ANIMATION_DELAYS = {
  SECTION_HEADER: 0,
  CARD_BASE: 0.2,
  CARD_STAGGER: 0.18,
  CTA: 1.1,
} as const;

/**
 * Defines the duration for various animations within the Projects section.
 */
export const PROJECT_ANIMATION_DURATIONS = {
  FADE_IN: 0.8,
  HOVER_TRANSITION: 0.5,
  SCALE_TRANSITION: 0.7,
} as const;
