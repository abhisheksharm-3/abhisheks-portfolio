"use client";

import { PageHeader } from "@/components/shared/PageHeader";

export const ContactHeader = () => (
  <PageHeader
    eyebrow="get in touch"
    title="let's connect"
    intro="open for freelance work, full-time roles, and the occasional wild idea."
  />
);
